import UIKit

enum NutritionModel {
    // MARK: Use cases
    struct Request {
        let text: String
    }
    
    struct Response: Decodable {
    }
    
    struct ViewModel {
        
    }
   
}
